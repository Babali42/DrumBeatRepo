name: Angular Tests

on:
  pull_request:
    branches: [ "main" ]
    paths: [ 'frontend/**', '.github/**' ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test
        uses: .github/actions/test.yml
